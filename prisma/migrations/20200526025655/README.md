# Migration `20200526025655`

This migration has been generated by Anand Chowdhary at 5/26/2020, 2:56:55 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `ara_v2`.`meetings` ALTER COLUMN `guests` DROP DEFAULT;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200521233555..20200526025655
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource mysql {
   provider = "mysql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 enum Gender {
   MALE
@@ -134,9 +134,9 @@
 model locations {
   id             Int           @default(autoincrement()) @id
   type           MeetingType
-  data           String?
+  data           Json?
   value          String
   createdAt      DateTime      @default(now())
   updatedAt      DateTime      @updatedAt
   meetings       meetings[]    @relation("meetingLocation")
@@ -149,9 +149,9 @@
   proposedTimes  String?
   confirmedTime  String?
   duration       Int
   meetingType    MeetingType       @default(VIDEO_CALL)
-  guests         String            @default("[]")
+  guests         Json
   language       String            @default("en")
   createdAt      DateTime          @default(now())
   updatedAt      DateTime          @updatedAt
   location       locations         @relation("meetingLocation", fields: [locationId], references: [id])
@@ -165,13 +165,13 @@
 model incoming_emails {
   id             Int           @default(autoincrement()) @id
   objectId       String        @unique
-  logs           String?
+  logs           Json?
   emailType      EmailType     @default(INCOMING)
-  from           String
-  to             String
-  cc             String
+  from           Json
+  to             Json
+  cc             Json
   subject        String
   status         EmailStatus   @default(PENDING)
   emailDate      DateTime
   messageId      String
```


