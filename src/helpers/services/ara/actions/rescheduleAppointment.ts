import { ActionParams } from "../../../../interfaces/ara";

export const rescheduleAppointment = async (params: ActionParams) => {
  throw new Error("I don't know how to reschedule an appointment");
};
